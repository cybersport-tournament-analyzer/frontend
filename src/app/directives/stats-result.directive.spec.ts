import { StatsResultDirective } from './stats-result.directive';

describe('StatsResultDirective', () => {
  it('should create an instance', () => {
    const directive = new StatsResultDirective();
    expect(directive).toBeTruthy();
  });
});
