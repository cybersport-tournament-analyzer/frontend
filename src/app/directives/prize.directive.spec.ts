import { PrizeDirective } from './prize.directive';

describe('PrizeDirective', () => {
  it('should create an instance', () => {
    const directive = new PrizeDirective();
    expect(directive).toBeTruthy();
  });
});
