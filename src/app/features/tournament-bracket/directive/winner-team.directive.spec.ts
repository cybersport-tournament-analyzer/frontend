import { WinnerTeamDirective } from './winner-team.directive';

describe('WinnerTeamDirective', () => {
  it('should create an instance', () => {
    const directive = new WinnerTeamDirective();
    expect(directive).toBeTruthy();
  });
});
