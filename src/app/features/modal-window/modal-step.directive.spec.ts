import { ModalStepDirective } from './modal-step.directive';

describe('ModalStepDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalStepDirective();
    expect(directive).toBeTruthy();
  });
});
